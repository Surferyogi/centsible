{
  "name": "centsible",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Centsible — monthly Japanese payslip monitor. On-device, offline-capable PWA.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "pdfjs-dist": "^4.6.82"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0",
    "gh-pages": "^6.1.1"
  }
}
